package com.battlecity.main.render;

import com.badlogic.gdx.graphics.Texture;
import com.battlecity.main.levels.BaseLevel;
import com.battlecity.main.net.Client;

/**
 *
 * @author JH62
 */
public class LevelRender extends Render {

    @Override
    public void render(IRenderable e) {
//        BaseLevel level = (BaseLevel) e;
//        Texture texture = level.texture;
//        float width = Client.instance().getWorld().getWidth();
//        float height = Client.instance().getWorld().getHeight();
//        batch.draw(texture, 0, 0, width, height);
    }
}
